# Deliberately small. The grader has to run `make setup && make eval` in under 15
# minutes, so no torch / sentence-transformers here: retrieval uses scikit-learn TF-IDF,
# with Gemini embeddings as an optional upgrade behind a config flag (see DECISIONS.md).
pandas>=2.0
scikit-learn>=1.3
google-generativeai>=0.8
groq>=0.11
python-dotenv>=1.0
pyyaml>=6.0
tqdm>=4.66
pytest>=8.0
