# secrets
.env
# raw + derived data (never commit the 500MB Kaggle dump)
data/raw/
data/interim/
*.sqlite
# LLM response cache (regenerable, but large)
.cache/
# python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/
# os
.DS_Store
.pytest_cache/
